---
description: 
globs: 
alwaysApply: false
---

# Your rule content

if in my prompt at the end i say/ type push code to gitlab
u should do this :

u must be in this directory : cd /home/thegodofcomputers/adivirtus-ai
then run git add .
then run git commit -m "descriptive message about the changes in the code that u did"
then run git push origin main
if it shows any error just force push the changes